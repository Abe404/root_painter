Saved test cases for annotation debugging.

Each .npz file contains: image (RGB), gt (ground truth), pred (model prediction).
Generated automatically when CHECK failures are detected, or manually via viewer notes.

Run: python -m sim_benchmark.test_annotator
